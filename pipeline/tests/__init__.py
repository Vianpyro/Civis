"""Python test suite. `python -m unittest discover -t . -s pipeline`, from the repo root.

A package rather than a loose directory: discovery only descends into packages,
and the tests import the pipeline the way the pipeline imports itself.
"""
